plugins {
    id 'java'
}

version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    jcenter()
    flatDir {
        dir 'lib'
    }
}

dependencies {
    compile group: 'algs4', name: 'algs4'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
